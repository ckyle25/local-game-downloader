<UserControl x:Class="LocalGameDownloader.DownloadQueueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Foreground="{DynamicResource TextBrush}"
             TextElement.Foreground="{DynamicResource TextBrush}"
             d:DesignHeight="420"
             d:DesignWidth="700">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="0,0,0,8"
                   FontWeight="SemiBold"
                   Text="{Binding SummaryText}" />

        <StackPanel Grid.Row="1"
                    Margin="0,0,0,8"
                    Orientation="Horizontal">
            <Button Width="90"
                    Margin="0,0,8,0"
                    Click="CancelButton_Click"
                    Content="Cancel"
                    IsEnabled="{Binding CanCancelSelected}" />
            <Button Width="90"
                    Margin="0,0,8,0"
                    Click="RetryButton_Click"
                    Content="Retry"
                    IsEnabled="{Binding CanRetrySelected}" />
            <Button Width="120"
                    Click="ClearButton_Click"
                    Content="Clear Finished" />
        </StackPanel>

        <DataGrid Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  ItemsSource="{Binding Jobs}"
                  SelectedItem="{Binding SelectedJob, Mode=TwoWay}">
            <DataGrid.Columns>
                <DataGridTextColumn Width="*" Binding="{Binding Entry.Name}" Header="Game" />
                <DataGridTextColumn Width="140" Binding="{Binding StateLabel}" Header="State" />
                <DataGridTextColumn Width="260" Binding="{Binding StatusText}" Header="Status" />
                <DataGridTemplateColumn Width="130" Header="Progress">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ProgressBar Minimum="0" Maximum="100" Value="{Binding ProgressValue}" Height="16" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
